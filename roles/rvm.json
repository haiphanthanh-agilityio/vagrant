{
  "name": "rvm",
  "chef_type": "role",
  "json_class": "Chef::Role",
  "description": "The role that used to install RVM, Ruby, Rails servers",
  "default_attributes": {
    "rvm": {
      "default_ruby": "2.1.2",
      "gems": {
        "ruby-2.1.2": [
          // { "name": "sass" },
          // { "name": "compass" }
        ]
      }
    }
  },

  "run_list": [
   
  ],

  "env_run_lists": {
    "development": [
      "recipe[rvm::vagrant]",
      "recipe[rvm::system]"
    ],
    "vagrant": [
      "recipe[rvm::vagrant]",
      "recipe[rvm::system]"
    ],
    "staging": [
      "recipe[rvm::system]"
    ]
  }
}